import { StyleSheet } from 'react-native'

import { hp } from '@/shared'

export const styles = StyleSheet.create({
  container: {
    zIndex: 2,
  },
  lottie: {
    position: 'absolute',
    top: 0,
    width: '100%',
    height: hp(80),
  },
})
