import { StyleSheet } from 'react-native'

import { EColors } from '../../styled'

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: EColors.neutral_500,
  },
})
