import { EScreens } from '../screens'

export const whiteList: Array<EScreens> = [
  EScreens.HomeMain,
  EScreens.ProfileMain,
  EScreens.CatalogMain,
  EScreens.FavoriteMain,
  EScreens.CartMain,
]
